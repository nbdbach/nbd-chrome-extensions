{
  "name": "nbd-auto-refresh",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Automatically reload the current tab on a schedule you choose.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json && tsx ../../scripts/copy-assets.ts auto-refresh"
  }
}
